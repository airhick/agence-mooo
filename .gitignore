# --- SECRETS: never commit (repo is served publicly at agence.mooo.com) ---
.env
.env.*

# --- DATA / PII: scraped business data incl. emails, must stay private ---
GMAPS_SCRAPPER_BUSINESS_With_Emails.csv
*.csv
!**/index_sample.csv

# --- Internal deliverables (not for public deploy) ---
audits/
state/
results.csv

# --- Python ---
.venv/
__pycache__/
*.pyc

# --- OS ---
.DS_Store

# --- Local preview / tooling artifacts (not for deploy) ---
.playwright-mcp/
preview-*.jpeg
preview-*.png
